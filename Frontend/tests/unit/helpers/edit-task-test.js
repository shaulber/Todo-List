import { editTask } from 'frontend/helpers/edit-task';
import { module, test } from 'qunit';

module('Unit | Helper | edit task');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = editTask([42]);
  assert.ok(result);
});
